export default function Configurations() {
    const API_IP_ADDRESS = "54.194.189.180"
    const API_PORT = "32026"

    const configData = {
        "API_BASE_URL": "http://" + API_IP_ADDRESS + ":" + API_PORT + "/",
    }

    return configData;
}